name: "modern_data_platform"
version: "1.0.0"
config-version: 2

profile: "modern_data_platform"

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  modern_data_platform:
    silver:
      +materialized: view
