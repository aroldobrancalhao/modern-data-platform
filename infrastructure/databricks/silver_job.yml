resources:
  jobs:
    silver:
      name: Silver Pipeline

      parameters:
        - name: entities
          default: customers
        - name: wheel_path
          default: ${workspace.root_path}/artifacts/.internal

      tasks:
        - task_key: silver

          notebook_task:
            notebook_path: ../../notebooks/silver/transform_silver.ipynb
